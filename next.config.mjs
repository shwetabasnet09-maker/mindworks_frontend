/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["127.0.0.1", "localhost"],
  },
};


export default nextConfig;
